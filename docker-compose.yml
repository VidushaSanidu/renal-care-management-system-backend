services:
  backend:
    image: node:22-alpine
    container_name: renal-backend

    working_dir: /app

    volumes:
      - .:/app
      - /app/node_modules

    ports:
      - "5000:5000"

    env_file:
      - .env

    command: npm run dev

    restart: unless-stopped
